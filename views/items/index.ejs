<% layout("/layouts/boilerplate") %>
<body>
    <h3>All Items</h3>
    <!-- <form action="/items/new" method="GET">
        <button>Add New Item</button>
    </form> -->
    <ol>
        <% for(let item of allItems){ %>
            <li><a href="/items/<%= item._id %>">
                <%= item.title %></a>
            </li>
            <% } %>
    </ol>
</body>